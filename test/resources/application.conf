slick.dbs.default = {
  profile = "slick.jdbc.PostgresProfile$"
  db.driver = "org.postgresql.Driver"
  db.url = "jdbc:postgresql:pantheon_test"
  db.user = "postgres"
  db.numThreads = 2
}

pantheon.auth.enabled = false

redis.enabled = false
redis.port = 6379
redis.message.channel = "cancel"
