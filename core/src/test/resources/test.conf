test.dbs {
  hsqldb-foodmart {
    enabled = true
  }
  postgresql {
    enabled = false
    //mandatory parameters
    database = "foodmart"
    host = "localhost"
    user = "foodmart"
    //optional parameters
    port = 5432
    password = ""
    // 'public' is the default
    //schema = "public"
  }
  clickhouse {
    enabled = false
    //mandatory parameters
    host = "localhost"
    //optional parameters
    user = "default"
    password = ""
    port = 8123
    database = "foodmart"
  }
  mysql {
    enabled = false
    //mandatory parameters
    host = ${?MYSQL_HOST}
    user = ${?MYSQL_USER}
    //optional parameters
    database = "foodmart"
    //port = "TODO: set as default explicitly for documentation purpose"
    password = ${?MYSQL_PASSWORD}
    protocol = "mysql"
  }
  hana {
    enabled = false
    //mandatory parameters
    host = ${?HANA_HOST}
    port = ${?HANA_PORT}
    user = ${?HANA_USER}
    //optional parameters
    password = ${?HANA_PASSWORD}
    schema = ${?HANA_SCHEMA}
    database = "foodmart"
  }
  db2 {
    enabled = false
    //mandatory parameters
    host = ${?DB2_HOST}
    user = "db2inst1"
    database = "foodmart"
    //optional parameters
    port = 50000
    password = "jhgfkuyfkuyt7647548"
  }
  oracle {
    enabled = false
    host = ${?ORACLE_HOST}
    user = ${?ORACLE_USER}
    port = ${?ORACLE_PORT}
    password = ${?ORACLE_PASSWORD}
    database = "XEPDB1"
  }
  teradata {
    enabled = false
    host = ${?TERADATA_HOST}
    user = ${?TERADATA_USER}
    port = ${?TERADATA_PORT}
    password = ${?TERADATA_PASSWORD}
    database = "foodmart"
  }
}
